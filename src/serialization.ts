/*
import { Board } from "./board";

export enum Serialize {
    Buffer,
    JSON,
}

let serialization = Serialize.JSON;

export function binarySerialization() {
    serialization = Serialize.Buffer;
}
export function jsonSerialization() {
    serialization = Serialize.JSON;
}

export function serialize(): Serialize {
    return serialization;
}

*/
export {}
